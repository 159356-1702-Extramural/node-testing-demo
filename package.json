{
  "name": "node_testing_demo",
  "version": "1.0.0",
  "description": "Just testing testing",
  "main": "app.js",
  "scripts": {
    
    // Unit test
    "test": "mocha",

    // Code coverage
    "cover": "istanbul cover _mocha",
    
    // pipes istanbul's report over to Coveralls
    "coveralls": "npm run cover -- --report lcovonly && cat ./coverage/lcov.info | coveralls"
  },
  "author": "Tim Ackroyd",
  "license": "ISC",
  "devDependencies": {
    "coveralls": "^2.13.1",
    "istanbul": "^0.4.5",
    "mocha": "^3.4.2",
    "mocha-lcov-reporter": "^1.3.0"
  }
}
